# Claude Flow 환경변수 설정
# 이 파일을 .env로 복사하여 사용하세요

# ============================================================================
# 필수 설정
# ============================================================================

# Claude Code OAuth Token (필수)
# Claude Code CLI에서 발급받은 토큰을 입력하세요
CLAUDE_CODE_OAUTH_TOKEN="your-token-here"

# Claude CLI 경로 (필수)
# claude CLI 실행 파일의 절대 경로를 입력하세요
# 예: /usr/local/bin/claude 또는 /Users/username/.nvm/versions/node/v24.5.0/bin/claude
CLAUDE_CLI_PATH="/path/to/claude"


# ============================================================================
# Worker Tool 타임아웃 설정 (선택, 단위: 초)
# ============================================================================

# Planner Agent 타임아웃 (기본값: 300초 = 5분)
# 요구사항 분석 및 계획 수립 작업의 최대 실행 시간
WORKER_TIMEOUT_PLANNER=300

# Coder Agent 타임아웃 (기본값: 600초 = 10분)
# 코드 작성, 수정, 리팩토링 작업의 최대 실행 시간
WORKER_TIMEOUT_CODER=600

# Reviewer Agent 타임아웃 (기본값: 300초 = 5분)
# 코드 리뷰 및 품질 검증 작업의 최대 실행 시간
WORKER_TIMEOUT_REVIEWER=300

# Tester Agent 타임아웃 (기본값: 600초 = 10분)
# 테스트 작성 및 실행 작업의 최대 실행 시간
WORKER_TIMEOUT_TESTER=600

# Committer Agent 타임아웃 (기본값: 180초 = 3분)
# Git 커밋 생성 작업의 최대 실행 시간
WORKER_TIMEOUT_COMMITTER=180


# ============================================================================
# 워크플로우 제한 설정 (선택)
# ============================================================================
# 참고: 이 설정들은 config/system_config.json에서도 조정 가능합니다

# 최대 Review → Coder → Review 사이클 반복 횟수 (기본값: 3)
# Critical 이슈 수정을 위한 최대 반복 횟수
# MAX_REVIEW_CYCLES=3

# 최대 Coder 재시도 횟수 (기본값: 2)
# Coder Agent 실행 실패 시 최대 재시도 횟수
# MAX_CODER_RETRIES=2


# ============================================================================
# 로깅 설정 (선택)
# ============================================================================

# 로그 레벨 (기본값: INFO)
# 옵션: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# 로그 출력 형식 (기본값: json)
# 옵션: json (구조화된 JSON 로그), console (사람이 읽기 쉬운 형식)
LOG_FORMAT=json

# 로그 파일 디렉토리 (기본값: logs)
LOG_DIR=logs

# 상세 로그 활성화 (기본값: false)
# Worker Tool 실행 시 상세한 디버그 정보 출력
# ENABLE_VERBOSE_LOGGING=false


# ============================================================================
# 성능 튜닝 (선택)
# ============================================================================

# Worker 재시도 로직 활성화 (기본값: true)
# Planner Agent에 재시도 로직 적용 여부
# WORKER_RETRY_ENABLED=true

# 재시도 최대 횟수 (기본값: 3)
# WORKER_RETRY_MAX_ATTEMPTS=3

# 재시도 기본 대기 시간 (기본값: 1.0초)
# WORKER_RETRY_BASE_DELAY=1.0


# ============================================================================
# 개발자 옵션 (선택)
# ============================================================================

# 메트릭 수집 비활성화 (기본값: false)
# Worker 실행 메트릭 수집 비활성화
# DISABLE_METRICS=false

# 에러 통계 자동 리셋 (기본값: false)
# 세션 시작 시 에러 통계 자동 초기화
# AUTO_RESET_ERROR_STATS=false
