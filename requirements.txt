# 그룹 챗 오케스트레이션 시스템 의존성
# Python 3.10 이상 필요

# Claude API (매니저 에이전트용)
anthropic>=0.18.0
claude-agent-sdk>=0.1.4

# CLI 프레임워크
click>=8.0.0

# TUI (Terminal User Interface)
textual>=0.47.0
rich>=13.7.0

# 유틸리티
python-dotenv>=1.0.0

# 구조화된 로깅
structlog>=23.1.0

# 템플릿 엔진
jinja2>=3.1.0
pyyaml>=6.0.0

# 문서화 (mkdocs)
mkdocs>=1.5.0
mkdocs-material>=9.0.0
mkdocstrings[python]>=0.24.0
pymdown-extensions>=10.0.0

# Vector Database 및 Embedding (Project Memory Bank)
faiss-cpu>=1.7.0
sentence-transformers>=2.2.0

# 개발 의존성 (선택사항)
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
ruff>=0.1.0
